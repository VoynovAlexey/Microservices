spring:
  application:
    name: service_b

  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}

  cloud:
    openfeign:
      client:
        config:
          messageClient:
            url: ${SERVICE_C_URL}

service-c:
  uri: ${SERVICE_C_URL}